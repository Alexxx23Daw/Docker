apiVersion: v1
kind: Service
metadata:
  name: mi-servicio-web
  labels:
    app: mi-app
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80         
      targetPort: http-server 
      nodePort: 30007  